# Workspace Instructions

- Keep the application focused on local network usage for command tickets and delivery status control.
- Prefer minimal, readable code and avoid unnecessary dependencies.
- Update the browser interface and the monitor dashboard together when changing order workflows.
- Keep the interface responsive so phones and a large display both work well.
- Use Portuguese for user-facing text unless the user asks otherwise.
